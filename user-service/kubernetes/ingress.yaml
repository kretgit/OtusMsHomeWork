# для сервиса аутентификации требуется отдельный ingress, но для упрощения деплоя все вынесено в один ingress

#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: auth-ingress
#spec:
#  ingressClassName: nginx
#  rules:
#    - host: arch.homework
#      http:
#        paths:
#          - path: /user
#            pathType: Prefix
#            backend:
#              service:
#                name: user-service
#                port:
#                  number: 8000